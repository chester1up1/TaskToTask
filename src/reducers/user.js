const defaultState = {
    load:true,
  }
    
  const user = (state = defaultState, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
    
    export default user;
    